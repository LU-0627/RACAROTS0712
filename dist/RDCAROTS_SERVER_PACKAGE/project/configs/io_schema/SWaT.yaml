# IO Schema for SWaT Dataset (Secure Water Treatment)

mode: explicit_io
description: "SWaT water treatment plant - actuators and sensors"

# Input variables (actuators, pumps, valves)
# Based on SWaT system architecture
input_indices: [0, 1, 2, 6, 7, 11, 12, 13, 14, 18, 19, 23, 24, 28, 29, 33, 34, 38, 39, 43, 44]

# Output variables (sensors, measurements)
output_indices: [3, 4, 5, 8, 9, 10, 15, 16, 17, 20, 21, 22, 25, 26, 27, 30, 31, 32, 35, 36, 37, 40, 41, 42, 45, 46, 47, 48, 49, 50]

# No ignored variables (all 51 variables used)
ignored_indices: []

notes: |
  SWaT dataset has 51 variables total.

  WARNING: This is a TEMPLATE. The actual input/output assignment should be
  verified against the SWaT dataset documentation:
  - Motor pumps and valve actuators → inputs
  - Level sensors, flow sensors, pressure sensors → outputs

  Please update this file after checking the actual column names in:
  SWaT_Dataset_Normal_v1.csv

  Column naming convention in SWaT:
  - MV* (motor valves) → likely inputs
  - P* (pumps) → likely inputs
  - FIT* (flow indicators/transmitters) → likely outputs
  - LIT* (level indicators/transmitters) → likely outputs
  - AIT* (analytical indicators) → likely outputs
